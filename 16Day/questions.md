# Preguntas

1. Los operadores de asignacion pueden ir por detras de los operadores aritmeticos? -> Falso, esto no funciona deben ir delantate 
2. Cuantos opradores de asignacion puedo tener a la hora de declarar una constante? -> 1 que deberia ser el igual y por obligacion, 
3. Como puedo hacer una exponenciacion de un numero? -> Con el operador `**`
4. Las operaciones aritmeticas en JavaScript tienen operadores de precedencia? -> Si primero debe hacerse lo que tenga mas precedencia como parentesis, multiplicaciones o divisiones, etc...
5. Quien tiene mayor precedencia en este enunciado : `(x + y ) * 5 / 2)` -> Lo que esta dentro del parentesis
6. Cual es este operador `??` -> Nullish coalescing assignment operator, que debe tener 2 valores, sirve para definir si el primer es un valor undefined o nulo y si lo es asigna el segundo valor
7. Cual es este operador? `...` -> El spread Operator/rest Operator sirve expandir un array o objeto en elementos individuales o agrupar multiples elementos en un solo array o objeto 
8. Para que me sirvne los condicional statements? -> nos permiten validar diferentes acciones o condiciones 
9. **UN SOLO OPERADOR TERNARIO** cuantas condiciones y expresiones puede recibir? -> Una condicion y 2 expresiones
10. Para que me sirve un break y un default en un switch? -> El break indica que cuando se cumpla o llegue a ese case salga y no siga tomando en cuenta los otros cases y el default es usado cuando ninguno de los casos se cumplio por defecto use el default
11. Cuantos default puede tener un swith? -> Solo puede tener 1 
12. Cuales son los unicos statements que me permiten saltar hacia afuera en un codigo de bloque? -> `break` y `continue`