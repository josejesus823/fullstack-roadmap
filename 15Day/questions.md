# Preguntas
1. En que etiqueta html debe ser insertado el javascript? -> Script
2.  un archivo javascript puede tener une extension .xjs? -> No solo js
3. Cual es el correcto atributo de la etiqueta script para permitir que un archivo js externo sea anclado al html? -> src
4. que haen estos 3 metodos? window.alert(), console.log(), document.write() -> window alert envia una alerta, console.log imprime en consola y document.write escribe en el documento
5. Es lo mismo decir alert(8) === window.alert(8)? -> Verdadero
6. Para que me sirve el document.write() -> Para escribir en el documento
7. Que etiqueta o etiquetas me sirven para cambiar el contenido de un HTML? -> Textcontent ,InnherHTML e InnerText
8. Cuando es recomendable solo usar innerText -> Cuando quiero solo cambiar texto plano
9. Hay un problema maluco con el document.write cual es? -> que cuando es usado se elimina todo el html existente
10. Es opcional el uso de window para disparar un alert? -> Falso, se puede saltar la palabra window ya que este objeto tiene un alcance global
11. Que es un hypen? un guion 
12. Multiples en una sola linea son contados como 1 solo statement? -> Falso, siguen contando como statements individuales


