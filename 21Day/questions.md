# Preguntas
1. Cuando ocurre un referenceError -> cuando se usa una variable que no existe
2. Cuando ocurre un RangeError? -> Cuando el valor esperado esta fuera del rango;
3. Cuando ocurre un UriError? -> Cuando se usan caracteres ilegales
4. Que es un error silencioso -> Son errores que no lanzas excepciones ni paran la ejecucion del codigo
5. La sentencia try ademas de funcion con el catch con que otras mas puede funcionar? -> Finally y Throw
6. Un bloque finally es obligatorio por que? -> Falso, este es opcional 
7. Que hace `debugger`? -> Para la ejecucion de JavaScript y llama la funcion de debugging si existe
8. Para que me sirve un onload ? -> Para avisar que el navegador ha terminado de cargar la pagina
9. el uso de 'strict mode' puede ser definido unicamente al inicio del js? -> Falso
10. Que me va a devolver JS si en un array busco una posicion que no existe? -> Undefined
